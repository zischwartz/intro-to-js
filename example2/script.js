$( document ).ready(function() {
    console.log( "ready!" );
    $("button#do_thing").on('click',function(e){
      // slide toggle the alert

      // get the value of the input
      // change the button's text to that value

    });
});